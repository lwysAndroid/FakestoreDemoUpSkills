package com.chemasmas.fakestoreapi.core.designSystem.previewsConfig

import androidx.compose.ui.tooling.preview.Preview

/**
 * Preview annotation that represents a phone device size. Add this annotation to a composable
 * to render a phone device.
 */
@Preview(name = "phone", device = "spec:shape=Normal,width=360,height=640,unit=dp,dpi=480")
annotation class PhonePreview
